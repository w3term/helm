releases:
  - name: w3term
    namespace: w3term
    chart: oci://ghcr.io/w3term/term
    version: 1.0.2
    values:
    - values.yaml
    secrets:
    - secrets.yaml

#releases:
#  - name: w3term
#    namespace: w3term
#    labels:
#      app: w3term
#    chart: .
#    values:
#    - values.yaml
#    secrets:
#    - secrets.yaml
