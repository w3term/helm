apiVersion: v1
kind: Service
metadata:
  name: auth
  labels:
    app.kubernetes.io/component: authentication
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/component: authentication
